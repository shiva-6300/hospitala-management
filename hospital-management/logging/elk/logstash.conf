# Minimal Logstash pipeline.
# Receives logs from Filebeat and forwards them to Elasticsearch.
input {
  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "hospital-logs-%{+YYYY.MM.dd}"
  }
}
